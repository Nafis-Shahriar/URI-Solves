#include<stdio.h>
int main()
{
    int m,n,i,a[20],b[20];
    while(scanf("%d%d",&n,&m)==2)
    {
        for(i=0;i<n;i++)
        {
            scanf("%d",&a[i]);
        }

        for(i=0;i<m;i++)
        {
            scanf("%d",&b[i]);
        }
    }
}
